# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @crypto-alert/scraper`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t crypto-alert-scraper`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system crypto-alert-scraper && \
          adduser --system -G crypto-alert-scraper crypto-alert-scraper

COPY apps/scraper crypto-alert-scraper/
COPY apps/scraper/package.json crypto-alert-scraper/
RUN chown -R crypto-alert-scraper:crypto-alert-scraper .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix crypto-alert-scraper --omit=dev -f install

CMD [ "node", "crypto-alert-scraper" ]
