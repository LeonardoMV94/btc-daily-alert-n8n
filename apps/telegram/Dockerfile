# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @crypto-alert/telegram`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t crypto-alert-telegram`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system crypto-alert-telegram && \
          adduser --system -G crypto-alert-telegram crypto-alert-telegram

COPY apps/telegram/dist crypto-alert-telegram/
COPY apps/telegram/package.json crypto-alert-telegram/
RUN chown -R crypto-alert-telegram:crypto-alert-telegram .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix crypto-alert-telegram --omit=dev -f install

CMD [ "node", "crypto-alert-telegram" ]
